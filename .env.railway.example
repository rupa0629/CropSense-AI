# Set these in the CropSense backend service on Railway. Never commit values.
ENVIRONMENT=production
PORT=8000
JWT_SECRET=
OPENAI_API_KEY=
OPENWEATHER_API_KEY=

# Replace both URLs after Vercel assigns the frontend domain.
FRONTEND_ORIGINS=https://your-project.vercel.app
FRONTEND_URL=https://your-project.vercel.app

# Generate a Railway public domain, then include it plus the healthcheck host.
ALLOWED_HOSTS=your-api.up.railway.app,healthcheck.railway.app,127.0.0.1,localhost

# Use a Railway PostgreSQL reference after completing docs/POSTGRESQL_MIGRATION.md.
DATABASE_URL=postgresql://USER:PASSWORD@HOST:PORT/DATABASE
RUN_DATABASE_MIGRATIONS=true
# One-time cutover variables. Remove after the verified transfer:
# MIGRATION_DATABASE_URL=${{Postgres.DATABASE_URL}}
# MIGRATE_SQLITE_TO_POSTGRES=true
# LEGACY_SQLITE_PATH=/app/data/cropsense.db
# RUN_BACKUP_REHEARSAL=true
# BACKUP_ENCRYPTION_KEY=${{secret(64)}}
# BACKUP_OUTPUT=/app/data/backups
MODEL_PATH=models/rice_disease_model.keras
ALLOW_HEURISTIC_FALLBACKS=false
FORCE_HTTPS=false

# Create a Railway Redis service named Redis and use reference variables:
REDIS_HOST=${{Redis.REDISHOST}}
REDIS_PORT=${{Redis.REDISPORT}}
REDIS_PASSWORD=${{Redis.REDISPASSWORD}}
REDIS_USE_RATE_LIMITING=true

METRICS_TOKEN=
SENTRY_DSN=
SENTRY_TRACES_SAMPLE_RATE=0.1
ANALYSIS_RETENTION_DAYS=730
WEATHER_RETENTION_DAYS=365
CHAT_RETENTION_DAYS=90
SMTP_HOST=
SMTP_PORT=587
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_FROM_EMAIL=
SMTP_USE_TLS=true
